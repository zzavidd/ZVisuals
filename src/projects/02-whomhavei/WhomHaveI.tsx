'use client';

import { Stack, Typography } from '@mui/material';

export default function WhomHaveI() {
  return (
    <Stack justifyContent={'center'} alignItems={'center'} height={'100%'}>
      <Typography color={'text.primary'} fontSize={52}>
        Whom Have I
      </Typography>
    </Stack>
  );
}
