import React from 'react';

export default function Head() {
  return (
    <React.Fragment>
      <title>ZVisuals</title>
      <meta content={'width=device-width, initial-scale=1'} name={'viewport'} />
      <link rel={'icon'} href={'/favicon.ico'} />
    </React.Fragment>
  );
}
